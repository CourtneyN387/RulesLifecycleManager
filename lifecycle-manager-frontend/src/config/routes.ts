// Ad the path of you page here so it can be used instead of a magic string
export const routes = {
  // i.e.
  // About: "/about",
  AddRule: "/add",
  ViewRules: "/",
  UpdateRule: "/update/:id",
} as const;
